# 实验记录

所有正式实验都必须填写 `template.csv` 中的字段。规则：

1. 使用唯一实验编号，如 `exp_001`。
2. 写入当前 Git commit。
3. 明确数据版本、fold 和标签来源。
4. 每次只改变一个关键因素，并填写 `唯一变量`。
5. 保存每类 AUC、Macro AUC、训练/推理时间和结论。
6. OOF、权重和日志放在 `outputs/`，不要提交到 Git。
